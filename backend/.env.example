# Local dev (no Docker): SQLite
# Path is relative to backend/prisma/schema.prisma → backend/prisma/dev.db
DATABASE_URL="file:./dev.db"

# Production / Docker: PostgreSQL
# DATABASE_URL="postgresql://exam:exam_secret@localhost:5432/exam_system?schema=public"

# Redis (exam timers, QR token cache, auto-save queue) — opt-in for local dev
# REDIS_ENABLED=true
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
JWT_SECRET="change-me-in-production-use-long-random-string"
JWT_EXPIRES_IN="8h"
JWT_REMEMBER_EXPIRES_IN="7d"
SESSION_INACTIVITY_MINUTES=30
PORT=3000
UPLOAD_DIR="./uploads"
CORS_ORIGIN="http://localhost:5173,http://127.0.0.1:5173"
# Add your Vercel frontend URL when deploying, e.g. https://your-app.vercel.app
FRONTEND_URL="http://localhost:5173"
QR_TOKEN_SECRET="change-me-qr-token-secret"
INTERNAL_API_KEY="change-me-internal-api-key"

# DeepSeek LLM auto-grading for short-answer questions (OpenAI-compatible API)
# When DEEPSEEK_API_KEY is set, short answers are graded on submit via keyword detection.
# Without a key, the system falls back to local keyword matching.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL="https://api.deepseek.com"
DEEPSEEK_MODEL="deepseek-v4-pro"
DEEPSEEK_TIMEOUT_MS=30000
